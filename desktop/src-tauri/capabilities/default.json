{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the NanoClaw window — webview-only shell, no Tauri APIs needed from JS.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
